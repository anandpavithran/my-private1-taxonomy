achine Learning (ML) is a branch of artificial intelligence that uses algorithms to 
identify patterns in data. In Supervised Learning, models are trained on labeled 
datasets. Unsupervised Learning finds hidden structures in unlabeled data. 
Reinforcement Learning trains agents to maximize rewards through trial and error.
